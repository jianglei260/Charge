<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wuxian.charge">

    <application
        android:name=".app.ChargeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.AccountLoginActivity" />
        <activity android:name=".login.QRCodeActivity" />
        <activity android:name=".login.FingerLoginActivity" />
        <activity android:name=".rent.RentActivity" />
        <activity android:name=".manage.ManageActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".login.InputFingerActivity"></activity>
    </application>

</manifest>